let firstName = "Aditya";
let roll = 52;
let lastName = "raj";

console.log(`Hello, My name is ${firstName} and my roll is ${roll} and last name is ${lastName}`);

const username = new String("menonduty");
let position = 4;
console.log(`The character at position ${position} is ${username.charAt(position)}`);
console.log(`The character at position ${position} is ${username.at(position)}`);
